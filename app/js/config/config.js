// TD  namespace
if (typeof TD === "undefined") {
  var TD = this.TD = {};
}

// config shared by all environments
TD.config = {
  version: "0.0.1",
  env: "dev" // available envs: dev, test, prod
};
